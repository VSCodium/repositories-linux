# [{{ project_name }}]({{ project_url }})

## Install on Debian (.deb)

### Supported Architectures

{% assign architectures = repo_arch_deb | split: " " %}
{% for architecture in architectures %}
- {{ architecture }}
{% endfor %}

### GPG Key

Add the GPG key of the repository:

```sh
curl -fsSL {{ repo_url }}/{{ repo_name }}.gpg \
  | gpg --dearmor \
  | sudo dd of=/usr/share/keyrings/{{ repo_name }}.gpg
```

### Repository

#### Debian 13 / Ubuntu 24.04 or newer

Add the `.sources` file:

```sh
sudo curl --output-dir /etc/apt/sources.list.d -LO {{ repo_url }}/{{ repo_name }}.sources
```

#### Debian 12 / Ubuntu 23.10 or older

Add the `.list` file:

```sh
sudo curl --output-dir /etc/apt/sources.list.d -LO {{ repo_url }}/{{ repo_name }}.list
```

### Install

Update then install {{ project_name }}:

```sh
sudo apt update && sudo apt install {{ package_name }}
```

## Install Fedora/Suse (.rpm)

### Supported Architectures

{% assign architectures = repo_arch_rpm | split: " " %}
{% for architecture in architectures %}
- {{ architecture }}
{% endfor %}

### Repository

Add the `.repo` file:

```sh
sudo curl --output-dir /etc/yum.repos.d -LO {{ repo_url }}/{{ repo_name }}.repo
```

### Install

Install {{ project_name }}:

#### Fedora/RHEL

```sh
sudo dnf install {{ package_name }}
```

#### OpenSuse/Suse

```sh
sudo zypper in {{ package_name }}
```
